# CI is disabled in this fork

All verification is performed locally (54 C++ unit tests, including raw GPU kernel
tests on an RX 480, plus end-to-end determinism checks). If you re-enable CI, copy
the workflow files from the upstream repository:

https://github.com/lightgbm-org/LightGBM/tree/main/.github/workflows
